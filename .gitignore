# El config.json local contiene el token de Telegram: NO debe subirse a GitHub
config.json
*.log
state.json.tmp
__pycache__/

# Despliegue Docker: datos de ejecucion y secretos (van en ./data y .env, no en git)
data/
.env

# state.json y usuarios.json ya NO son estado de git: el servicio escribe en ./data
# (volumen). Siguen versionados por compatibilidad; para dejar de versionarlos:
#   git rm --cached state.json usuarios.json
state.json
usuarios.json

# Archivos del proyecto de prompts de opencode (no forman parte del vigilante)
prompt/
README.es.md
opencode.jsonc
